BM25 index doesn't persist/reload after restart (silent hybrid-search degradation)
Cross-encoder reranker shows domain mismatch on academic text — occasionally misranks boilerplate above content (trained on MS MARCO, not scientific papers)
Boilerplate stripping is line-based and can leave orphaned fragments when a phrase wraps across multiple PDF-extracted lines